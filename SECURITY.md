# 安全政策

## 支持的版本

| 版本 | 支持状态 |
| ---- | -------- |
| 2.3.x | ✅ 支持 |
| < 2.3 | ❌ 不支持 |

## 报告漏洞

我们非常重视安全问题。如果您发现安全漏洞，请通过以下方式报告：

### 报告渠道

**请勿在公开 Issue 中报告安全漏洞。**

请发送邮件至：security@example.com

### 报告内容

请包含以下信息：

1. **漏洞描述**：清晰描述漏洞的性质
2. **影响范围**：哪些组件/版本受到影响
3. **复现步骤**：详细的复现步骤
4. **概念验证**：如果可能，提供 PoC 代码
5. **影响评估**：漏洞的潜在影响
6. **修复建议**：如果您有修复建议

### 响应时间

- **确认收到**：24 小时内
- **初步评估**：72 小时内
- **修复时间**：根据严重程度，7-30 天

### 严重程度分类

| 严重程度 | 描述 | 响应时间 |
|----------|------|----------|
| 严重 | 远程代码执行、认证绕过 | 7 天 |
| 高 | 敏感数据泄露、权限提升 | 14 天 |
| 中 | CSRF、信息泄露 | 30 天 |
| 低 | 非敏感信息泄露 | 90 天 |

## 安全设计原则

### 认证与授权

- 所有用户密码使用 Argon2id 哈希
- Session 管理支持单活动会话
- 设备认证使用 HMAC-SHA256
- 敏感操作需要二次认证

### 数据保护

- 所有敏感数据加密存储 (AES-256-GCM)
- 密钥用途隔离
- 日志中不记录敏感信息
- 数据库使用 SQLite WAL 模式

### 网络安全

- 所有 API 通信使用 HTTPS
- CSRF 保护
- CORS 限制
- 请求速率限制

### 进程安全

- 最小权限原则
- 文件权限严格控制
- 无任意命令执行
- 进程隔离

## 安全配置建议

### 生产环境

1. **使用 HTTPS**
   - 配置有效的 TLS 证书
   - 启用 HSTS
   - 禁用不安全的 TLS 版本

2. **网络隔离**
   - 控制面板仅监听 localhost
   - 使用反向代理暴露公网接口
   - 配置防火墙规则

3. **访问控制**
   - 使用强密码
   - 启用二次认证
   - 限制管理员数量

4. **监控与审计**
   - 启用审计日志
   - 监控异常登录
   - 定期审查访问日志

### Docker 部署

```bash
# 使用非 root 用户运行
docker run --user 1000:1000 ...

# 限制资源
docker run --memory=512m --cpus=1 ...

# 只读文件系统
docker run --read-only ...

# 限制能力
docker run --cap-drop=ALL --cap-add=NET_BIND_SERVICE ...
```

## 安全更新

我们定期发布安全更新。建议：

1. 订阅 GitHub Release 通知
2. 定期检查更新
3. 及时应用安全补丁

## 安全审计

本项目定期进行安全审计：

- 代码审查
- 依赖漏洞扫描
- 渗透测试

## 致谢

我们感谢以下安全研究人员的贡献：

（暂无）

## 许可证

本安全政策适用于项目的所有版本。
